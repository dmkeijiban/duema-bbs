name: Seed Cron (disabled)

# 自動コメント生成を停止
# on:
#   workflow_dispatch:
#   schedule:
#     - cron: '0 22 * * *'
#     - cron: '0 3 * * *'
#     - cron: '0 10 * * *'
#     - cron: '0 13 * * *'

on:
  workflow_dispatch:  # 手動実行のみ残す（テスト用）

jobs:
  seed:
    runs-on: ubuntu-latest
    if: false  # 常にスキップ
    steps:
      - name: Call seed API
        run: |
          curl -f -X GET https://www.duema-bbs.com/api/seed \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}"
